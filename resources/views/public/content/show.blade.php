@extends('layouts.public', ['title'=>$item->{$titleField}.' · '.$school?->name, 'description'=>Str::limit(strip_tags($item->{$descriptionField}),160)])
@section('content')
<article><header class="bg-emerald-950 px-5 pb-20 pt-36 text-white"><div class="mx-auto max-w-4xl"><a href="{{ route('content.index',$module) }}" class="text-sm font-bold uppercase text-emerald-300">{{ $label }}</a><h1 class="mt-5 text-4xl font-black sm:text-6xl">{{ $item->{$titleField} }}</h1></div></header><div class="mx-auto max-w-3xl px-5 py-16"><div class="space-y-6 text-lg leading-9 text-slate-700">@foreach(preg_split('/\r\n|\r|\n/',$item->{$descriptionField}) as $paragraph)@if(trim($paragraph))<p>{{ $paragraph }}</p>@endif @endforeach</div>@if($module==='agenda')<dl class="mt-10 grid gap-4 rounded-2xl bg-white p-6 shadow-sm sm:grid-cols-2"><div><dt class="text-xs text-slate-500">Waktu</dt><dd class="font-bold">{{ $item->starts_at->translatedFormat('d F Y H:i') }}</dd></div><div><dt class="text-xs text-slate-500">Lokasi</dt><dd class="font-bold">{{ $item->location??'Akan diumumkan' }}</dd></div></dl>@endif</div>@if($module==='galeri' && $item->items->isNotEmpty())<div class="mx-auto grid max-w-6xl gap-4 px-5 pb-20 sm:grid-cols-2 lg:grid-cols-3">@foreach($item->items as $galleryItem)<figure class="overflow-hidden rounded-2xl bg-white shadow-sm">@if($galleryItem->file_path)<img src="{{ Storage::url($galleryItem->file_path) }}" loading="lazy" class="aspect-square w-full object-cover" alt="{{ $galleryItem->caption }}">@else<a href="{{ $galleryItem->video_url }}" target="_blank" rel="noopener" class="grid aspect-video place-items-center bg-slate-900 p-5 text-center font-bold text-white">Tonton video</a>@endif @if($galleryItem->caption)<figcaption class="p-4 text-sm text-slate-600">{{ $galleryItem->caption }}</figcaption>@endif</figure>@endforeach</div>@endif</article>
@endsection
