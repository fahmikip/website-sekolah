@extends('layouts.public', ['title'=>'Berita Sekolah · '.$school?->name, 'description'=>'Berita, kegiatan, dan prestasi terbaru '.$school?->name])
@section('content')
<section class="bg-emerald-950 px-5 pb-20 pt-36 text-white lg:px-8"><div class="mx-auto max-w-7xl"><p class="text-sm font-bold uppercase tracking-[.2em] text-emerald-300">Ruang informasi</p><h1 class="mt-4 text-5xl font-black tracking-tight">Berita sekolah</h1><p class="mt-4 max-w-2xl text-lg text-emerald-100">Ikuti kegiatan, gagasan, dan capaian terbaru dari warga sekolah.</p></div></section>
<section class="px-5 py-16 lg:px-8"><div class="mx-auto max-w-7xl"><form class="grid gap-3 rounded-2xl bg-white p-4 shadow-sm sm:grid-cols-[1fr_220px_auto]"><input name="search" value="{{ request('search') }}" placeholder="Cari berita..." class="rounded-xl border-slate-200"><select name="category" class="rounded-xl border-slate-200"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select><button class="rounded-xl bg-emerald-800 px-6 py-3 font-bold text-white">Cari</button></form><div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($news as $item)<article class="group overflow-hidden rounded-3xl bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><a href="{{ route('news.show',$item->slug) }}"><div class="aspect-video bg-gradient-to-br from-emerald-700 to-slate-900">@if($item->featured_image)<img src="{{ Storage::url($item->featured_image) }}" loading="lazy" class="h-full w-full object-cover" alt="">@else<div class="grid h-full place-items-center text-5xl font-black text-white/30">{{ mb_substr($item->title,0,1) }}</div>@endif</div><div class="p-6"><div class="text-xs font-bold uppercase tracking-wider text-emerald-700">{{ $item->category->name }} · {{ $item->published_at->translatedFormat('d M Y') }}</div><h2 class="mt-3 text-xl font-black leading-snug group-hover:text-emerald-800">{{ $item->title }}</h2><p class="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{{ $item->excerpt }}</p><div class="mt-5 text-sm font-bold text-emerald-800">Baca selengkapnya →</div></div></a></article>@empty<div class="rounded-3xl bg-white p-12 text-center text-slate-500 md:col-span-2 lg:col-span-3">Belum ada berita yang sesuai pencarian.</div>@endforelse</div><div class="mt-10">{{ $news->links() }}</div></div></section>
@endsection
