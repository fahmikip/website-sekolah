@extends('layouts.admin')
@section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-bold text-emerald-700">PHASE 5</p><h1 class="text-3xl font-black">e-Rapor</h1></div><form method="POST" action="{{ route('admin.reports.generate') }}" class="flex gap-2">@csrf<select name="classroom_id" required class="rounded-xl border-slate-300"><option value="">Pilih rombel</option>@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}">{{ $classroom->name }}</option>@endforeach</select><button class="rounded-xl bg-emerald-700 px-4 py-2 font-bold text-white">Hitung rapor</button></form></div>
<div class="mt-6 overflow-hidden rounded-2xl bg-white shadow"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Siswa</th><th>Rombel</th><th>Semester</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($reports as $report)<tr class="border-t"><td class="p-4 font-semibold">{{ $report->student->name }}</td><td>{{ $report->classroom->name }}</td><td>{{ $report->semester->name }}</td><td><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold">{{ strtoupper($report->status) }}</span></td><td><a class="text-emerald-700 underline" href="{{ route('admin.reports.show',$report) }}">Kelola</a></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">Belum ada rapor. Pilih rombel untuk menghitung.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $reports->links() }}</div>
@endsection
