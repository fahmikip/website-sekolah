@extends('layouts.admin', ['title'=>'Berita', 'heading'=>'Berita'])
@section('content')
<div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-2xl font-black">Kelola berita</h2><p class="mt-1 text-sm text-slate-500">Draft, jadwalkan, dan publikasikan kabar sekolah.</p></div>@can('create_news')<a href="{{ route('admin.news.create') }}" class="rounded-full bg-emerald-700 px-6 py-3 text-center text-sm font-bold text-white">Tulis berita</a>@endcan</div>
<form class="mb-5 grid gap-3 rounded-2xl bg-white p-4 shadow-sm sm:grid-cols-[1fr_180px_auto]"><input name="search" value="{{ request('search') }}" placeholder="Cari judul atau ringkasan..." class="rounded-xl border-slate-200"><select name="status" class="rounded-xl border-slate-200"><option value="">Semua status</option>@foreach(['draft'=>'Draft','published'=>'Published','scheduled'=>'Scheduled','archived'=>'Archived'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-5 py-2 text-sm font-bold text-white">Filter</button></form>
<div class="overflow-hidden rounded-2xl bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full min-w-[800px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th class="p-4">Berita</th><th class="p-4">Kategori</th><th class="p-4">Status</th><th class="p-4">Publikasi</th><th class="p-4 text-right">Aksi</th></tr></thead><tbody class="divide-y">@forelse($news as $item)<tr><td class="p-4"><div class="font-bold">{{ $item->title }}</div><div class="mt-1 text-xs text-slate-500">oleh {{ $item->author?->name ?? 'Pengguna dihapus' }} · {{ number_format($item->view_count) }} dilihat</div></td><td class="p-4">{{ $item->category->name }}</td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $item->status==='published'?'bg-emerald-100 text-emerald-700':'bg-amber-100 text-amber-700' }}">{{ ucfirst($item->status) }}</span></td><td class="p-4">{{ $item->published_at?->format('d M Y H:i') ?? '—' }}</td><td class="p-4"><div class="flex justify-end gap-2">@can('edit_news')<a href="{{ route('admin.news.edit',$item) }}" class="rounded-lg border px-3 py-2">Edit</a>@endcan @can('delete_news')<form method="POST" action="{{ route('admin.news.destroy',$item) }}" onsubmit="return confirm('Pindahkan berita ini ke sampah?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-600">Hapus</button></form>@endcan</div></td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-slate-500">Belum ada berita yang sesuai filter.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $news->links() }}</div>
@endsection
