@extends('layouts.admin',['title'=>$label,'heading'=>$label]) @section('content')<div class="mb-6 flex items-center justify-between"><div><h2 class="text-2xl font-black">{{ $label }}</h2><p class="text-sm text-slate-500">Master data akademik terpusat.</p></div><a href="{{ route('admin.academic.create',$module) }}" class="rounded-full bg-emerald-700 px-6 py-3 font-bold text-white">Tambah</a></div><form class="mb-5 flex gap-3"><input name="search" value="{{ request('search') }}" class="flex-1 rounded-xl border-slate-200" placeholder="Cari..."><button class="rounded-xl bg-slate-900 px-5 text-white">Cari</button></form><div class="divide-y rounded-2xl bg-white shadow-sm">@forelse($items as $item)<div class="flex items-center justify-between p-5"><div><b>{{ $item->{$title} ?: ('Jadwal #'.$item->id) }}</b><p class="text-xs text-slate-500">ID {{ $item->id }} · diperbarui {{ $item->updated_at->diffForHumans() }}</p></div><div class="flex gap-2"><a class="rounded-lg border px-3 py-2" href="{{ route('admin.academic.edit',[$module,$item->id]) }}">Edit</a><form method="POST" action="{{ route('admin.academic.destroy',[$module,$item->id]) }}" onsubmit="return confirm('Hapus data?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-600">Hapus</button></form></div></div>@empty<div class="p-10 text-center">Belum ada data.</div>@endforelse</div><div class="mt-5">{{ $items->links() }}</div>@endsection
