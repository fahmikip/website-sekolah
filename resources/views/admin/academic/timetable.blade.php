@extends('layouts.admin',['title'=>'Jadwal Pelajaran','heading'=>'Jadwal Pelajaran'])
@section('content')
<form class="grid gap-3 rounded-2xl bg-white p-5 shadow-sm sm:grid-cols-4"><select name="academic_year_id" class="rounded-xl border-slate-200"><option value="">Tahun ajaran aktif</option>@foreach($academicYears as $year)<option value="{{ $year->id }}" @selected($yearId==$year->id)>{{ $year->name }}</option>@endforeach</select><select name="semester_id" class="rounded-xl border-slate-200"><option value="">Semester aktif</option>@foreach($semesters as $semester)<option value="{{ $semester->id }}" @selected($semesterId==$semester->id)>{{ $semester->name }}</option>@endforeach</select><select name="classroom_id" class="rounded-xl border-slate-200"><option value="">Semua rombel</option>@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}" @selected(request('classroom_id')==$classroom->id)>{{ $classroom->name }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-5 py-3 font-bold text-white">Terapkan filter</button></form>
<div class="mt-6 grid gap-5 lg:grid-cols-3">@foreach([1=>'Senin',2=>'Selasa',3=>'Rabu',4=>'Kamis',5=>'Jumat',6=>'Sabtu'] as $day=>$name)<section class="rounded-2xl bg-white p-5 shadow-sm"><h2 class="text-lg font-black">{{ $name }}</h2><div class="mt-4 space-y-3">@forelse($schedules->get($day,collect()) as $item)<article class="rounded-xl border-l-4 border-emerald-500 bg-slate-50 p-4"><div class="flex justify-between gap-3"><b>{{ $item->subject->name }}</b><span class="text-xs">{{ substr($item->starts_at,0,5) }}–{{ substr($item->ends_at,0,5) }}</span></div><p class="mt-1 text-sm text-slate-500">{{ $item->classroom->name }} · {{ $item->teacher->name }}</p><p class="text-xs text-slate-400">{{ $item->room }}</p></article>@empty<p class="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">Tidak ada jadwal.</p>@endforelse</div></section>@endforeach</div>
@endsection
