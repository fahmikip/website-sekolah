@extends('layouts.admin',['title'=>'Asesmen','heading'=>'Manajemen Asesmen'])
@section('content')<div class="flex items-center justify-between"><div><h2 class="text-2xl font-black">Asesmen</h2><p class="text-sm text-slate-500">Kelola asesmen, komponen, peserta, dan publikasi nilai.</p></div><a href="{{ route('admin.assessments.create') }}" class="rounded-full bg-emerald-700 px-5 py-3 font-bold text-white">Buat asesmen</a></div><div class="mt-6 divide-y rounded-2xl bg-white shadow-sm">@forelse($assessments as $assessment)<article class="flex flex-wrap items-center justify-between gap-4 p-5"><div><b>{{ $assessment->title }}</b><p class="text-sm text-slate-500">{{ $assessment->subject->name }} · {{ $assessment->classroom->name }} · {{ $assessment->components_count }} komponen · {{ $assessment->student_scores_count }} siswa</p></div><div class="flex gap-2"><a href="{{ route('admin.assessment.scores.edit',$assessment) }}" class="rounded-lg border px-3 py-2">Nilai</a><a href="{{ route('admin.assessments.edit',$assessment) }}" class="rounded-lg border px-3 py-2">Edit</a><form method="POST" action="{{ route('admin.assessments.destroy',$assessment) }}" onsubmit="return confirm('Hapus asesmen?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-600">Hapus</button></form></div></article>@empty<p class="p-10 text-center">Belum ada asesmen.</p>@endforelse</div><div class="mt-5">{{ $assessments->links() }}</div>@endsection
