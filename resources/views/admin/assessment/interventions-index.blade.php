@extends('layouts.admin',['title'=>$label,'heading'=>$label])
@section('content')<div class="flex items-center justify-between"><div><h2 class="text-2xl font-black">{{ $label }}</h2><p class="text-sm text-slate-500">Tindak lanjut hasil asesmen yang tercatat dan terukur.</p></div><a href="{{ route('admin.assessment.interventions.create',$module) }}" class="rounded-full bg-emerald-700 px-5 py-3 font-bold text-white">Tambah</a></div><div class="mt-6 divide-y rounded-2xl bg-white shadow-sm">@forelse($items as $item)<article class="flex flex-wrap items-center justify-between gap-3 p-5"><div><b>{{ $item->student->name }}</b><p class="text-sm text-slate-500">{{ $module==='remedials' ? $item->assessment->title.' · '.$item->old_score.' → '.($item->final_score??'proses') : $item->subject->name.' · '.$item->activity }}</p></div><div class="flex gap-2"><a href="{{ route('admin.assessment.interventions.edit',[$module,$item]) }}" class="rounded-lg border px-3 py-2">Edit</a><form method="POST" action="{{ route('admin.assessment.interventions.destroy',[$module,$item]) }}" onsubmit="return confirm('Hapus data?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-600">Hapus</button></form></div></article>@empty<p class="p-10 text-center text-slate-500">Belum ada data.</p>@endforelse</div><div class="mt-5">{{ $items->links() }}</div>@endsection
