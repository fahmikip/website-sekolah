@extends('layouts.admin',['title'=>$label,'heading'=>$label])
@section('content')
<div class="mb-6 flex items-center justify-between"><div><h2 class="text-2xl font-black">{{ $label }}</h2><p class="text-sm text-slate-500">Konfigurasi penilaian tanpa nilai hardcode.</p></div><a href="{{ route('admin.assessment.config.create',$module) }}" class="rounded-full bg-emerald-700 px-6 py-3 font-bold text-white">Tambah</a></div>
<div class="divide-y rounded-2xl bg-white shadow-sm">@forelse($items as $item)<div class="flex items-center justify-between p-5"><div><b>{{ $item->{$title} }}</b><p class="text-xs text-slate-500">Diperbarui {{ $item->updated_at->diffForHumans() }}</p></div><div class="flex gap-2"><a href="{{ route('admin.assessment.config.edit',[$module,$item->id]) }}" class="rounded-lg border px-3 py-2">Edit</a><form method="POST" action="{{ route('admin.assessment.config.destroy',[$module,$item->id]) }}">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-600">Hapus</button></form></div></div>@empty<div class="p-10 text-center text-slate-500">Belum ada konfigurasi.</div>@endforelse</div><div class="mt-5">{{ $items->links() }}</div>
@endsection
