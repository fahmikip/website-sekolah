@extends('layouts.admin')
@section('content')
<div class="flex flex-wrap items-end justify-between gap-3"><div><p class="text-sm font-bold text-emerald-700">ANALISIS ASESMEN</p><h1 class="text-3xl font-black">{{ $assessment->title }}</h1></div><div class="flex gap-2"><a href="{{ route('admin.assessment.analysis.export',$assessment) }}" class="rounded-xl border bg-white px-4 py-2">Export Excel</a><a href="{{ route('admin.assessment.scores.edit',$assessment) }}" class="rounded-xl border px-4 py-2">Kembali</a></div></div>
<div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-6">@foreach(['Rata-rata'=>$stats['average'],'Median'=>$stats['median'],'Tertinggi'=>$stats['highest'],'Terendah'=>$stats['lowest'],'Tuntas'=>$stats['passed'],'Remedial'=>$stats['needs_remedial']] as $label=>$value)<article class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-xs text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-black">{{ $value }}</p></article>@endforeach</div>
@php($bands=['90–100'=>$scores->whereBetween('final_score',[90,100])->count(),'80–89'=>$scores->whereBetween('final_score',[80,89.99])->count(),'70–79'=>$scores->whereBetween('final_score',[70,79.99])->count(),'< 70'=>$scores->where('final_score','<',70)->count()])
<section class="mt-6 rounded-2xl bg-white p-6 shadow-sm"><h2 class="font-bold">Distribusi nilai</h2><div class="mt-5 space-y-4">@foreach($bands as $label=>$count)<div><div class="mb-1 flex justify-between text-sm"><span>{{ $label }}</span><b>{{ $count }} siswa</b></div><div class="h-3 rounded-full bg-slate-100"><div class="h-3 rounded-full bg-emerald-500" style="width:{{ $scores->count()?($count/$scores->count()*100):0 }}%"></div></div></div>@endforeach</div></section>
<section class="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Siswa</th>@foreach($assessment->components as $component)<th class="p-4">{{ $component->name }}</th>@endforeach<th class="p-4">Akhir</th><th>Status</th></tr></thead><tbody>@forelse($scores as $score)<tr class="border-t"><td class="p-4 font-semibold">{{ $score->student->name }}</td>@foreach($assessment->components as $component)<td class="p-4 text-center">{{ optional($score->details->firstWhere('assessment_component_id',$component->id))->score ?? '—' }}</td>@endforeach<td class="p-4 text-center font-black">{{ $score->final_score }}</td><td class="p-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $score->final_score >= $stats['passing']?'bg-emerald-100 text-emerald-800':'bg-red-100 text-red-800' }}">{{ $score->final_score >= $stats['passing']?'Tuntas':'Remedial' }}</span></td></tr>@empty<tr><td colspan="20" class="p-10 text-center">Belum ada nilai.</td></tr>@endforelse</tbody></table></div></section>
@endsection
