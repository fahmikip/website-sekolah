@extends('layouts.admin',['title'=>'Input Nilai','heading'=>'Input Nilai'])
@section('content')<div class="mb-6 flex flex-wrap justify-between gap-3"><div><h2 class="text-2xl font-black">Daftar Asesmen</h2><p class="text-sm text-slate-500">Pilih asesmen untuk membuka spreadsheet nilai.</p></div><div class="flex flex-wrap gap-2">@can('manage_assessment')<a href="{{ route('admin.assessments.index') }}" class="rounded-xl border bg-white px-4 py-2 font-semibold">Kelola asesmen</a><a href="{{ route('admin.assessment.interventions.index','remedials') }}" class="rounded-xl border bg-white px-4 py-2 font-semibold">Remedial</a><a href="{{ route('admin.assessment.interventions.index','enrichments') }}" class="rounded-xl border bg-white px-4 py-2 font-semibold">Pengayaan</a>@endcan</div></div><div class="space-y-4">@forelse($assessments as $assessment)<a href="{{ route('admin.assessment.scores.edit',$assessment) }}" class="flex items-center justify-between rounded-2xl bg-white p-6 shadow-sm"><div><h2 class="font-bold">{{ $assessment->title }}</h2><p class="text-sm text-slate-500">{{ $assessment->assessment_date }} · {{ $assessment->student_scores_count }} siswa</p></div><span class="rounded-full bg-emerald-100 px-4 py-2 text-sm font-bold text-emerald-700">Input nilai →</span></a>@empty<div class="rounded-2xl bg-white p-10 text-center">Belum ada asesmen.</div>@endforelse</div><div class="mt-5">{{ $assessments->links() }}</div>@endsection
